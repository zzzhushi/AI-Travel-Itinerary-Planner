The UI has been removed. Run the itinerary planner from the Windows CLI and get output in text files. See the project README:

```cmd
python run_itinerary.py --trip path\to\trip.json --activities path\to\activities.csv --output-dir output
```
